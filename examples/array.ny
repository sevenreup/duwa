nambala myArray = [1, 4, 6];

console.lemba(myArray[1]);
console.lemba("\n");
console.lemba("Length: " + myArray.length());
console.lemba("\n");

za (nambala i = 0; i < myArray.length(); i++) {
    console.lemba(myArray[i]);
    console.lemba("\n");
}